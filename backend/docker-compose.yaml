version: '3.8'

services:
  # web:
  #   build: .
  #   container_name: recipe_backend
  #   command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
  #   volumes:
  #     - .:/app
  #   ports:
  #     - "8000:8000"
  #   env_file:
  #     - .env
  #   environment:
  #     # Формуємо правильний URL для SQLAlchemy
  #     # Docker візьме ${POSTGRES_USER} з файлу .env
  #     # А хост ми примусово ставимо host.docker.internal
  #     - DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@host.docker.internal:${POSTGRES_SERVER}/${POSTGRES_DB}
      
  #     # Redis налаштування
  #     - REDIS_HOST=redis_cache
  #     - REDIS_PORT=6379
  #   extra_hosts:
  #     - "host.docker.internal:host-gateway"
  #   depends_on:
  #     - redis_cache

  # Redis (Кеш) - запускаємо локально для цього проекту
#   redis_cache:
#     image: redis:alpine
#     container_name: recipe_redis
#     command: redis-server --appendonly yes
#     volumes:
#       - redis_data:/data
#     ports:
#       - "6379:6379" # відкриваємо порт назовні
#     healthcheck:
#       test: ["CMD", "redis-cli", "ping"]
#       interval: 5s
#       timeout: 3s
#       retries: 5

# volumes:
#   redis_data: